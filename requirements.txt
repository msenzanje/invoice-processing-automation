# Core data contract
pydantic>=2.0

# Orchestration (LangGraph state machine)
langgraph>=0.2.0
langchain>=0.2.0

# Ingestion — format-specific extraction
pdfplumber>=0.10.0
PyMuPDF>=1.23.0
pandas>=2.0.0

# LLM transport
httpx>=0.27.0

# Config + CLI presentation
python-dotenv>=1.0.0
rich>=13.0.0

# Web dashboard (dashboard/ — FastAPI served, polled by a static frontend)
fastapi>=0.110.0
uvicorn>=0.27.0

# Tests
pytest>=8.0.0
